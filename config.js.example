// Copy to config.js for local development (config.js is gitignored).
// Production: Vercel sets API_BASE_URL and runs scripts/generate-config.js at build time.
window.ENV = {
  API_BASE_URL: 'http://localhost:8002',
};
